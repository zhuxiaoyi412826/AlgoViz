<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.algoviz.mapper.OrderMapper">
    
    <resultMap id="OrderResultMap" type="com.algoviz.entity.Order">
        <id column="id" property="id" />
        <result column="order_id" property="orderId" />
        <result column="product_id" property="productId" />
        <result column="product_name" property="productName" />
        <result column="amount" property="amount" />
        <result column="user_id" property="userId" />
        <result column="status" property="status" />
        <result column="wechat_trade_no" property="wechatTradeNo" />
        <result column="wechat_transaction_id" property="wechatTransactionId" />
        <result column="create_time" property="createTime" />
        <result column="pay_time" property="payTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>
    
    <select id="getOrderById" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE order_id = #{orderId}
    </select>
    
    <select id="getOrdersByUserId" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>
    
    <select id="getAllOrders" resultMap="OrderResultMap">
        SELECT * FROM orders ORDER BY create_time DESC
    </select>
    
    <select id="getOrdersByStatus" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE status = #{status} ORDER BY create_time DESC
    </select>
    
    <insert id="insertOrder">
        INSERT INTO orders (order_id, product_id, product_name, amount, user_id, status, create_time, update_time)
        VALUES (#{orderId}, #{productId}, #{productName}, #{amount}, #{userId}, #{status}, #{createTime}, #{updateTime})
    </insert>
    
    <update id="updateOrderStatus">
        UPDATE orders SET 
            status = #{status},
            update_time = CURRENT_TIMESTAMP
        WHERE order_id = #{orderId}
    </update>
    
    <update id="updateOrderPaymentInfo">
        UPDATE orders SET 
            wechat_trade_no = #{wechatTradeNo},
            wechat_transaction_id = #{wechatTransactionId},
            status = 'SUCCESS',
            pay_time = CURRENT_TIMESTAMP,
            update_time = CURRENT_TIMESTAMP
        WHERE order_id = #{orderId}
    </update>
    
    <delete id="deleteOrder">
        DELETE FROM orders WHERE order_id = #{orderId}
    </delete>
</mapper>